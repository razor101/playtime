@model PlayTime.Web.Models.Customer.CustomerCRUDModel

<h1>@Model.Name - edit</h1>

@using (Html.BeginForm("Edit", "Customer", FormMethod.Post))
{
    <h1>
        @Html.EditorFor(x => x.Name)
    </h1>
    
    <input type="submit" value="Update"/>
}

<p>
    @Html.ActionLink("Cancel", "View", "Customer", new { @id = Model.Id }, null)
</p>